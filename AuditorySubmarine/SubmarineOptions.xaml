<UserControl x:Class="LSRI.Submarine.SubmarineOptions"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:sdk="http://schemas.microsoft.com/winfx/2006/xaml/presentation/sdk" 
    xmlns:toolkit="http://schemas.microsoft.com/winfx/2006/xaml/presentation/toolkit"  
    xmlns:data="clr-namespace:LSRI.AuditoryGames.GameFramework.Data;assembly=GameFramework"
    xmlns:gamefw="clr-namespace:LSRI.AuditoryGames.GameFramework.UI;assembly=GameFramework"
    mc:Ignorable="d"
    d:DesignHeight="466" d:DesignWidth="484" Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <data:UserModelContainer x:Key="ContainerObject" />
    </UserControl.Resources>


    <Grid x:Name="LayoutRoot">
        <gamefw:SuperDataForm Height="361" HorizontalAlignment="Left" Name="_xPeople" VerticalAlignment="Top" Width="284" AutoCommit="False" AutoEdit="True" CommandButtonsVisibility="Navigation, Commit, Cancel" CancelButtonContent="Cancel" CommitButtonContent="Commit" HeaderVisibility="Collapsed" Header="User Profiles" AutoGenerateFields="True" ItemsSource="{Binding Source={StaticResource ContainerObject}, Path=UserModels}" DataContext="{Binding}" UseLayoutRounding="True">
           <!--
            <toolkit:DataForm.EditTemplate>
                <DataTemplate>
                    <StackPanel>
                        <toolkit:DataField>
                            <TextBox Text="{Binding Name, Mode=TwoWay}" />
                        </toolkit:DataField>
                        <toolkit:DataField>
                            <TextBox Text="{Binding FrequencyTraining, Mode=TwoWay}" />
                        </toolkit:DataField>
                        <toolkit:DataField >
                            <ItemsControl x:Name="icX" ItemsSource="{Binding GatesOpening2}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>

                                        <StackPanel Orientation="Horizontal">

                                        </StackPanel>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Width="70" Margin="5">
                                            
                                            <StackPanel>
                                                <TextBlock Margin="3,3,3,0" FontSize="12" HorizontalAlignment="Center"
                   Text="{Binding Path=Length}"/>
                                                <TextBox Margin="3,0,3,7" FontSize="12" HorizontalAlignment="Center"
                   Text="{Binding}"/>
                                            </StackPanel>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>

                            </ItemsControl>
                        </toolkit:DataField>
                
                    </StackPanel>
                </DataTemplate>
            </toolkit:DataForm.EditTemplate>  -->

        </gamefw:SuperDataForm>
        <Button Content="OK" Height="23" HorizontalAlignment="Left" Margin="12,431,0,0" Name="btnOK" VerticalAlignment="Top" Width="75" Click="btnOK_Click" />
    </Grid>
</UserControl>
